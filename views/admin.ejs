<%- include("templates/header", { authenticated, user_type }) %>

<h2>Admin Dashboard</h2>

<p>Total users: <%= users.length %></p>

<ul class="list-group">
  <% users.forEach(user => { %>
    <%- include("templates/user", { user }) %>
  <% }) %>
</ul>

<%- include("templates/footer") %>
